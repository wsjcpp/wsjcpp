# Build && Install on Ubuntu/Debian

## Requirements

```
$ sudo apt install libzip-dev 
```

## Build

```
$ ./build_simple.sh
```

## Install

Install (just a copy wsjcpp to /usr/bin/)

```
$ ./install.sh
```